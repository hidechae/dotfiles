snippet init_component
	'use strict';
	var React = require('react'),
			Router = require('react-router'),
			Link = Router.Link,
			Bootstrap = require('react-bootstrap')
	;

	var ${1} = React.createClass({
		render: function() {
			return (
				<div>
					${3}
				</div>
			);
		}
	});

	module.exports = ${2};

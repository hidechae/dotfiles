snippet rcc
    var React = require('react');

    var ${1} = React.createClass({

      render: function() {
        return (
          <div>
            ${3}
          </div>
        );
      }

    });

    module.exports = ${2};
